# Copia este archivo a .env y coloca TU token personal de ClickUp.
#   cp .env.example .env
#
# Cómo obtener tu token: ClickUp → (tu avatar) Settings → Apps → API Token → Generate.
# Formato: pk_xxxxxxxx. La herramienta solo lee tus tareas; nunca modifica nada.
CLICKUP_API_KEY=
